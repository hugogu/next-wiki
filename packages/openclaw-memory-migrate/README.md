# next-wiki OpenClaw Memory Migration

One-time migration utility for selected OpenClaw Markdown memory files. Run discovery and preview first, then explicitly approve a resumable import. The encrypted local ledger stores fingerprints and idempotency keys; source files are never deleted or modified. This package is separate from the continuous memory bridge.
