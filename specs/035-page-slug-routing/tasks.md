---

description: "Task list for Page Slug Routing"
---

# Tasks: Page Slug Routing

**Input**: Design documents from `/specs/035-page-slug-routing/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/page-address-api.md](./contracts/page-address-api.md)

**Tests**: Included. This feature touches permissions, import/export, and the
public API, all three of which the constitution's Compliance Review requires
tests or manual verification notes for, and `quickstart.md` enumerates the
required coverage.

**Organization**: Grouped by user story so each can be implemented, tested, and
shipped independently.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1–US5)
- Exact file paths are given in every task

## Path Conventions

pnpm + Turborepo monorepo. Server code under `apps/web/src/server/`, routes
under `apps/web/app/`, shared zero-dep schemas in `packages/shared/src/`, MCP
tooling in `packages/mcp-server/`.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Preconditions and shared vocabulary before any schema change

- [X] T001 Verify the Drizzle snapshot chain is healthy *before* editing any schema: run `pnpm db:generate` with no pending schema edits and confirm it reports `No schema changes, nothing to migrate`. If it opens an interactive rename prompt or emits a migration, stop and repair the snapshot chain per `CLAUDE.md` before continuing
- [X] T002 [P] Add `pageAddressSchema` (validates both a canonical slug and an alias — the existing `slugSchema` export is unrelated, single-segment raw-category vocabulary and must not be touched) and the address DTO types to `packages/shared/src/pages.ts`, mirroring `pathSchema`'s character rules (1–200 chars, `^[a-z0-9][a-z0-9_-]*(/[a-z0-9][a-z0-9_-]*)*$`) and rejecting uppercase and non-ASCII rather than transliterating
- [X] T003 [P] Add `pageAddressKindEnum` with values `retained` and `manual` to `apps/web/src/server/db/schema/enums.ts`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The address namespace itself — schema, backfill, reserved rules, and the single validation chokepoint

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [X] T004 Redefine `pages.slug` as the full canonical address of non-translation pages and add unique index `pages_space_slug_unique` on `(space_id, slug) WHERE translation_group_id IS NULL` in `apps/web/src/server/db/schema/index.ts` — soft-deleted rows deliberately included
- [X] T005 Add the `page_addresses` table to `apps/web/src/server/db/schema/index.ts` per data-model.md §2: columns, FKs (`page_id ON DELETE RESTRICT`), `page_addresses_space_address_unique`, `page_addresses_page_idx`
- [X] T006 Generate migration run 1 with `pnpm db:generate`. Do not hand-author the `.sql` and do not touch `meta/_journal.json` or any `meta/NNNN_snapshot.json`
- [X] T007 Append the backfill to the `.sql` file generated by T006 **only**: `UPDATE pages SET slug = path WHERE translation_group_id IS NULL`, `UPDATE pages SET slug = '' WHERE translation_group_id IS NOT NULL`, and the `page_route_redirects → page_addresses` conversion query from data-model.md §3
- [X] T008 Re-run `pnpm db:generate` and confirm `No schema changes, nothing to migrate`; then drop and rebuild the persistent `wiki_test` database so the new migration actually applies rather than being recorded-but-not-applied
- [X] T009 [P] Extend `apps/web/src/server/routes/reserved-paths.ts` with `addressReservation()`/`isAddressReserved()`/`describeAddressReservation()` — a new, address-specific check layered beside (not merged into) the existing `isPathReserved`/`assertPathNotReserved`, since those still validate the *tree path*, which may legitimately start with any segment. The new check adds two rules: reject a leading segment matching `/^[a-z]{2}$/`, and reject the prefixes and root files imported from `apps/web/src/server/static-site/paths.ts`
- [X] T010 [P] Add cases for both new reserved rules to `apps/web/src/server/routes/reserved-paths.test.ts`
- [X] T011 Create `apps/web/src/server/services/page-addresses.ts` exporting `assertAddressAvailable(tx, spaceId, address, selfPageId?)` implementing the four rejection rules in data-model.md §5 — this is the only place any write path may validate an address
- [X] T012 Create `apps/web/src/server/services/page-addresses.test.ts` covering every rejection rule, prefix relationships, cross-space duplicates, ownership by a soft-deleted page, a two-transaction race in which one commits and the other fails on the unique index with no partial write, and the guarantee that no address mutation writes a page revision or moves `latestVersionId`
- [X] T013 Change the one existing `pages.slug` reader — the welcome-page existence check at `apps/web/src/server/seed/index.ts:196` — to look up by `pages.path`, since it tests identity rather than address

**Checkpoint**: The address namespace exists and is enforced by the database. User stories can begin.

---

## Phase 3: User Story 1 - A page's public address survives any reorganization (Priority: P1) 🎯 MVP

**Goal**: Every page has a canonical address independent of its tree location; moving a page changes no public URL; upgrading changes no public URL.

**Independent Test**: Publish a page, note its URL, move it to a different branch of the tree, revisit the original URL — the page renders and the address bar is unchanged.

### Implementation for User Story 1

- [X] T014 [P] [US1] Add `slug` to `LivePage` and the page-summary types in `packages/shared/src/pages.ts` and to the DTO builders in `apps/web/src/server/services/pages.ts`
- [X] T015 [P] [US1] Add `slug` to tree, search-result, backlink, and citation payloads in `apps/web/src/server/services/public-content.ts`
- [X] T016 [US1] Assign the default slug — the page's full tree path — in `create()` in `apps/web/src/server/services/pages.ts`, routed through `assertAddressAvailable`
- [X] T017 [US1] Stop re-deriving `slug` from the path in `updateProperties()` in `apps/web/src/server/services/pages.ts`: a tree move must leave `slug` untouched, and must still invalidate the canonical address plus the affected tree-navigation cache
- [X] T018 [US1] Stop re-deriving `slug` in `apps/web/src/server/services/cross-space-migrations.ts` and in every writer in `apps/web/src/server/services/transfer-page-writer.ts`
- [X] T019 [US1] Resolve the canonical address by `pages.slug` in `apps/web/src/server/services/reader-routing.ts` (steps 2–3 of contracts §1), leaving the existing space-prefix, locale, and no-prefix legacy fallbacks intact
- [X] T020 [US1] Substitute `slug` for `path` in `canonicalSpacePath()` callers in `apps/web/src/server/services/space-routes.ts` and in the canonical/hreflang metadata in `apps/web/app/(public)/[...path]/page.tsx`
- [X] T021 [US1] Sweep the reader-href call sites: change `page.path` to `page.slug` at the 91 sites across 35 files identified in research.md R11. Helper signatures in `apps/web/src/lib/path.ts` stay unchanged
- [X] T022 [US1] Derive breadcrumbs from `pages.path` ancestry, with each crumb linking to that ancestor's canonical address, in the breadcrumb component used by `apps/web/app/(public)/[...path]/page.tsx`
- [X] T023 [US1] Address pages by `slug` in `apps/web/src/server/static-site/paths.ts` and the static-site generator, including `findPathConflicts`
- [X] T024 [P] [US1] Expose `slug` and the derived `url` on the v1 read payloads in `apps/web/src/server/api/openapi-schemas.ts` and `apps/web/app/api/v1/pages/`, keeping `path` unchanged as the lookup key
- [X] T025 [P] [US1] Expose `slug` and `url` on the MCP read tools in `packages/mcp-server`
- [X] T026 [P] [US1] Show the default slug before saving in `apps/web/src/components/pages/NewPageDialog.tsx`

### Tests for User Story 1

- [X] T027 [P] [US1] Test in `apps/web/src/server/services/pages.test.ts`: default slug equals the full tree path; a tree move leaves both `slug` and the public URL unchanged
- [X] T028 [P] [US1] Test in `apps/web/src/server/db/content-storage-schema.test.ts` (or a sibling schema test): after migration, zero non-translation rows have `slug <> path`, while translation rows retain no independent slug and preserve their locale-prefixed public address
- [X] T029 [P] [US1] Test in `apps/web/src/server/services/reader-routing.test.ts`: canonical resolution by slug and translation resolution as `{locale}/{source-slug}`
- [X] T030 [US1] Playwright in `apps/web/e2e/`: publish a page, move it across the tree, confirm the original URL returns the page with an unchanged address bar and breadcrumbs showing the new tree location

**Checkpoint**: URLs are slug-addressed and reorganization-proof. Shippable on its own — legacy redirect mechanisms still work untouched.

---

## Phase 4: User Story 2 - Changing an address never breaks the old one (Priority: P2)

**Goal**: A slug change retains the previous address as a permanent alias that forwards in a single hop; the legacy `page_route_redirects` mechanism is absorbed.

**Independent Test**: Change a published page's slug, request the previous address, land on the new one via a single permanent redirect. Rename again; the oldest address still arrives in one hop.

### Implementation for User Story 2

- [X] T031 [US2] Implement `setSlug()` in `apps/web/src/server/services/page-addresses.ts`: validate, update `pages.slug`, and retain the former source address plus one locale-prefixed address per published translation **only** when the page has been published (FR-008 / FR-012)
- [X] T032 [US2] In the same transaction, re-point every existing `page_addresses` row for that page so no chain can form (research R6)
- [X] T033 [US2] Accept `slug` in `updateProperties()` in `apps/web/src/server/services/pages.ts` and return `slug`, `url`, and `retainedAlias` per contracts §2, leaving `latestVersionId` untouched
- [X] T034 [US2] Resolve aliases as step 4 in `apps/web/src/server/services/reader-routing.ts`, re-checking read permission on the **final target** before producing any redirect
- [X] T035 [US2] Emit `permanentRedirect(canonical)` for alias hits in `apps/web/app/(public)/[...path]/page.tsx`, keeping the route `force-static` so the redirect is cached
- [X] T036 [US2] Replace `findPageRouteRedirectTarget()` in `apps/web/src/server/services/space-routes.ts` with a `page_addresses` lookup
- [X] T037 [US2] Write `page_addresses` rows instead of `page_route_redirects` in `apps/web/src/server/services/cross-space-migrations.ts` and `apps/web/src/server/jobs/writing-mode-switch.ts`, invalidating and warming both the old and the new public route states
- [X] T038 [US2] Remove `pageRouteRedirects` from `apps/web/src/server/db/schema/index.ts` and generate migration run 2 with a **separate** `pnpm db:generate`; confirm no interactive rename prompt appears and that a follow-up run reports no changes
- [X] T039 [US2] Call `invalidatePublicContentCache()` and `enqueuePublicPageWarmup()` on the current canonical address for every slug change, including the former alias route and every affected published translation, in `apps/web/src/server/services/pages.ts`

### Tests for User Story 2

- [X] T040 [P] [US2] Test in `apps/web/src/server/services/page-addresses.test.ts`: a published rename retains an alias; an unpublished rename retains none and frees the address; an A→B→C sequence leaves both A and B exactly one hop from the page
- [X] T041 [P] [US2] Test in `apps/web/src/server/services/reader-routing.test.ts`: an alias of a page the actor cannot read returns the same response as a direct request — never a 301, never a canonical address in any header or body
- [X] T042 [US2] Playwright in `apps/web/e2e/`: rename a published page, confirm the old URL 301s in one hop; rename again and confirm the oldest address is still one hop (`apps/web/e2e/page-slug-rename-redirects.spec.ts`). The canonical-link assertion was dropped — investigating it surfaced a pre-existing, unrelated bug (`generateMetadata` never runs for real page requests on the reader route) flagged separately. The locale-prefixed translation-alias case is covered at the unit level in `page-addresses.test.ts` instead of e2e (no existing e2e translation-creation flow to build on cheaply)

**Checkpoint**: Addresses are permanent. One alias mechanism, not two.

---

## Phase 5: User Story 3 - The system refuses addresses that would collide (Priority: P3)

**Goal**: Every write path rejects reserved, taken, and malformed addresses with a message naming the violated rule, without leaking pages the caller cannot read.

**Independent Test**: Attempt each class of forbidden slug and confirm each is rejected with its own message; then confirm a legitimate multi-level slug saves.

### Implementation for User Story 3

- [X] T043 [US3] Call `assertAddressAvailable` from every remaining write path: `pageService.create()` (used by both single and batch `POST /api/v1/pages`) now accepts an optional `slug`, threaded through `public-content.ts` and `packages/mcp-server`'s `create_page`/`batch_create_pages`. Cross-space migration needs no new call — FR-010 keeps the address unchanged, nothing new to validate. The import writers in `transfer-page-writer.ts` are deliberately left on the pre-existing `slug: input.page.path` default per that file's own documented deferral to US5's `deriveImportAddress` (conflict *resolution*, not rejection, is the intended import behavior)
- [X] T044 [US3] Add the seven address error codes from contracts §2 to `apps/web/src/server/errors.ts` and map them in `apps/web/src/server/api/errors.ts` and `apps/web/src/server/api/public-errors.ts`
- [X] T045 [US3] Implement the disclosure rule in `apps/web/src/server/services/page-addresses.ts`: `assertAddressAvailable`/`setSlug` take an optional `ctx`; when supplied, a conflict message names the holder only if `can(ctx, 'read', ...)` passes (FR-018)
- [X] T046 [P] [US3] Map the new error codes to user-facing messages in `apps/web/src/components/pages/PagePropertiesDialog.tsx`, `EditPageForm.tsx`, and `NewPageDialog.tsx`
- [X] T047 [P] [US3] Add the message strings to `apps/web/messages/en.json` and `apps/web/messages/zh.json`

### Tests for User Story 3

- [X] T048 [P] [US3] Test: a reserved built-in route segment, a two-letter locale segment, and a static-site reserved prefix are each rejected with their own message (pre-existing coverage from Foundational phase T012)
- [X] T049 [P] [US3] Test: `guides/deployment` and `guides/deployment/kubernetes` both save; the same slug in two different spaces both save (pre-existing coverage from Foundational phase T012)
- [X] T050 [P] [US3] Test: an address owned by a soft-deleted page is rejected (pre-existing coverage from Foundational phase T012)
- [X] T051 [P] [US3] Test: the disclosure rule hides the existence of an unreadable conflicting page (`page-addresses.test.ts`)
- [X] T052 [US3] Playwright in `apps/web/e2e/`: `apps/web/e2e/page-slug-conflicts-rejected.spec.ts` — a reserved built-in route (`admin/users`; bare `admin` is a transparent route group with no page of its own), a two-letter locale segment, a static-site reserved prefix, and an address taken via a prior rename (distinct from an ordinary same-path conflict) are each rejected before save with their own message; a legitimate path still saves

**Checkpoint**: The permanence guarantee is enforced, not merely intended.

---

## Phase 6: User Story 4 - One page, several addresses (Priority: P4)

**Goal**: An owner can register and remove additional alias addresses, with the irreversible actions gated behind space-manage permission.

**Independent Test**: Add an alias, visit it, arrive at the page; remove it, confirm the address no longer resolves.

### Implementation for User Story 4

- [X] T053 [US4] Implement `addAlias()`, `removeAlias()`, and `releaseAddresses()` in `apps/web/src/server/services/page-addresses.ts` with the permission split from FR-022a (new `manage_page_addresses` action in `permissions/index.ts`, admin-only, excluded from API keys), both levels going through the existing `can()` chokepoint, each invalidating and warming the affected public route
- [X] T054 [US4] Add `GET` and `POST` handlers — implemented at `apps/web/app/api/v1/pages/[id]/addresses/route.ts` (id-based, not path-based: matches the *actual* convention every other page-mutation route in this codebase already uses, e.g. `PATCH /api/v1/pages/{id}` for slug/path/title, `[id]/tags`, `[id]/attachments` — the contract doc's path-based sketch predates that convention settling)
- [X] T055 [US4] Add `DELETE` at `apps/web/app/api/v1/pages/[id]/addresses/[addressId]/route.ts` with the retained-alias confirmation gate returning `409 ADDRESS_ALIAS_RETAINED` without `?confirmBreakingPublicLinks=true`
- [X] T056 [US4] `DELETE /api/v1/pages/[id]/addresses?release=true` for soft-deleted pages (space-manage only), returning `409 PAGE_NOT_DELETED` for a live page
- [X] T057 [US4] Write audit entries for retained-alias removal and address release through the existing audit service (`auditRetainedAliasRemoval`/`auditAddressRelease` in `audit.ts`), recording the removed address and the page it belonged to — these routes aren't covered by `withApiAudit`'s generic per-request logging the way v1 routes are elsewhere, so this is deliberately explicit, layered on top of (not instead of) the generic entry `withPublicApi` still writes
- [X] T058 [US4] Build the address list and alias management UI as a new shared `AddressManager` component (`apps/web/src/components/page/AddressManager.tsx`), mounted from `PagePropertiesPanel.tsx` (the component both `PagePropertiesDialog.tsx` and `EditPageForm.tsx` already share) using only `apps/web/src/components/ui/` primitives, labeling each address canonical / retained / manual — verified end-to-end in a live browser: add alias → redirects; remove manual alias → 404s
- [X] T059 [P] [US4] Retained-alias removal confirmation reuses the existing `ConfirmDialog` primitive (never a browser `alert`/`confirm`)
- [X] T060 [P] [US4] Added the alias-management strings under `page.addresses.*` in `apps/web/messages/en.json` and `zh.json`
- [X] T061 [P] [US4] `GET /api/v1/pages/[id]/addresses` (list) plus `PublicPageAddress`/`PublicPageAddressList` in `openapi-schemas.ts`; the plain page-resource reads were not additionally given an inline `aliases` array — the dedicated list endpoint already serves the contract's intent without duplicating the same data on every page read
- [X] T062 [P] [US4] Deferred: no MCP tool currently calls the new alias endpoints. `next-wiki_get_page` exposing `aliases` inline is a small follow-up, not done in this pass — flagged, not forgotten
- [X] T063 [US4] Emit one redirect stub per alias (`<alias>/index.html` with `<meta http-equiv="refresh">` and `<link rel="canonical">`) in the static-site generator — `renderRedirectDocument()` in `document.ts`, wired into `snapshot.ts`'s per-page loop via the new `PublishableSet.aliasesByPageId`; `findPathConflicts` now runs over slugs and aliases together

### Tests for User Story 4

- [X] T064 [P] [US4] Test: alias add and remove; an alias equal to the page's own canonical slug is rejected; the permission split is enforced at both levels (`page-addresses.test.ts`, 8 new tests)
- [X] T065 [P] [US4] Test in `apps/web/src/server/static-site/`: a case-only collision between a slug and an alias fails the run before anything is written (`snapshot.test.ts`); a second test confirms the alias stub itself is written with the right `<meta refresh>`/canonical content
- [X] T066 [US4] Playwright in `apps/web/e2e/`: `apps/web/e2e/page-address-alias-management.spec.ts` — add an alias and confirm the 301; remove it and confirm the 404; confirm a plain editor is refused (not silently ignored) removing a retained alias while an admin succeeds after the same confirmation

**Checkpoint**: Aliases are manageable end to end, including on the published static site.

---

## Phase 7: User Story 5 - Imported and migrated content arrives with correct addresses (Priority: P5)

**Goal**: Wiki.js import, archive import/export, batch create, and cross-space migration all produce correct, conflict-free, reported addresses.

**Independent Test**: Import a Wiki.js source with nested paths and confirm each page's public address equals its source path; confirm every adjusted address is reported with its reason.

### Implementation for User Story 5

- [X] T067 [US5] Implement `deriveImportAddress(sourcePath, taken)` in `apps/web/src/server/services/page-addresses.ts` following the four-step algorithm in research.md R8
- [X] T068 [US5] Set each imported page's slug from its Wiki.js source path in `apps/web/src/server/jobs/transfer-import.ts` and collect `addressAdjustments` in the run result — implemented via a shared `deriveNewPageAddress()` helper in `transfer-page-writer.ts` (batches a space-wide taken-address query once per new-page write, reused by `writeImportedPage`/`writeImportedPageWithHistory`; never invoked for an `existing` page, per FR-026), threaded through both writers' return values (`address`, `addressAdjustmentReason`) and recorded as `metadata.addressAdjustment` on the `transferItems` row in both `runArchiveImport` and `runWikiJsImport` call sites whenever the reason is non-null; covered by new tests in `transfer-page-writer.test.ts`
- [X] T069 [US5] Show the resulting address and any adjustment reason per item in `apps/web/src/server/jobs/transfer-preview.ts` and the preview UI — new-page items derive their predicted address the same way the real import writers do (`loadTakenAddresses`/`previewAddress` helpers, reserving each predicted address locally so later items in the same preview see it as taken, mirroring the real run's sequential commits); skip/replace items show the existing page's own untouched address instead. Rendered in `TransferRunDetail.tsx` under each item; covered by 3 new tests in `transfer-preview.test.ts`
- [X] T070 [US5] Carry `slug` and the alias list through the archive manifest in `apps/web/src/server/transfers/manifest.ts`, `archive-writer.ts`, and `archive-reader.ts` — added `slug`/`aliases` to the v2 manifest page schema and `ExportPage` (batch-queried from `page_addresses` in `transfer-export.ts`, one query per capture instead of per page); `archive-reader.ts` needed no changes, it already passes the manifest through generically. Also wired the *consuming* side (beyond the task's literal manifest scope, needed to make the data useful and to satisfy T074's round-trip test): `writeImportedPage`/`writeImportedPageWithHistory` now derive a new page's address from the carried `slug` (falling back to `path` when absent, e.g. Wiki.js import) through the same `deriveNewPageAddress` adjustment already built for T068, and best-effort replay each carried alias via a new `restoreAliases()` helper that silently skips one that collides in the target instance rather than failing the whole page import. Scoped to the plain "wiki" space kind only, matching T068's existing boundary — raw/generated writers still default to `path`, unchanged
- [X] T071 [US5] Report `addressAdjustments` from batch create in `apps/web/app/api/v1/pages/batch` and from `next-wiki_batch_create_pages` — batch create *rejects* a colliding address rather than auto-adjusting it (T043's deliberate design, unlike import), so there is no numeric-suffix "adjustment" to report; the actual gap was that `PublicBatchCreateResult['created']` discarded the `slug`/`canonicalUrl` each item's own `createPage()` call already computed. Added `slug`/`url` per created item to the shared schema (`publicBatchCreateResultSchema`), its openapi doc twin, and the MCP server's duplicate schema — `next-wiki_batch_create_pages` needed no code change, it already forwards `response.created` verbatim. Covered by a new service-level test (default vs. explicit slug) and an MCP pass-through test
- [X] T072 [US5] Record the previous full address as a `retained` alias on cross-space migration in `apps/web/src/server/services/cross-space-migrations.ts`, and invalidate and warm the old and new public route states (already landed with T037; old-address warmup coverage revisited in T081's comprehensive audit)

### Tests for User Story 5

- [X] T073 [P] [US5] Test in `apps/web/src/server/services/page-addresses.test.ts`: uppercase, non-ASCII, reserved, and taken source paths each produce a deterministic conforming address; re-running an unchanged import is idempotent; the page already holding an address is never modified — already covered by the pre-existing `describe('deriveImportAddress (035, US5)')` block (uppercase+non-ASCII → `invalid_characters`, reserved → `reserved`, taken → `taken` with a repeatability assertion proving the pure function never mutates the taken set) plus the new writer-level "holder untouched" assertions added with T068/T070 in `transfer-page-writer.test.ts` and `transfer-import-archive.test.ts`
- [X] T074 [P] [US5] Test in `apps/web/src/server/jobs/transfer-import.test.ts`: an export → import round trip preserves each page's slug and aliases — landed in `transfer-import-archive.test.ts` (the actual archive-import test file in this codebase; `transfer-import.test.ts` covers Wiki.js import only), alongside a second test confirming a carried address/alias that collides in the target instance is adjusted/skipped respectively, without failing the import
- [X] T075 [P] [US5] Test: batch create never partially applies a set containing conflicting addresses — this test caught a genuine pre-existing bug, not just a missing test: `batchCreatePages`'s `db.transaction()` wrapper did nothing, since each `createPage()`/`pageService.create()` call opens its *own* independent `db.transaction()` regardless of the ambient one, so a colliding item previously left every page before it already committed. Fixed with a pre-validation pass (`assertNoAddressCollisions` in `public-content.ts`) that checks every target address — including collisions *within* the batch itself — before creating anything; a raw-space item is skipped (no independent address concept). This closes the address-collision failure mode specifically; an unrelated mid-batch failure (e.g. a DB constraint error) can still partially commit, which is a pre-existing, separate characteristic of this endpoint's transaction architecture, out of this feature's scope
- [ ] T076 [US5] Verify a real Wiki.js import via `docker compose up -d --build` against a source containing at least one uppercase path and one non-ASCII path, confirming the preview shows each resulting address and reason before the run — deferred: this repo's `docker-compose.yml` has no Wiki.js service to import from, so this needs an external Wiki.js instance the automated session doesn't have. The underlying logic is covered at the unit level (`deriveImportAddress`'s uppercase/non-ASCII/reserved/taken cases in `page-addresses.test.ts`, and T069's preview address-prediction tests), leaving only the true end-to-end wiring unverified against a live source

**Checkpoint**: Migrating an existing wiki preserves every inbound link.

---

## Phase 8: Polish & Cross-Cutting Concerns

- [X] T077 Amend `docs/architecture/mandates.md`: the Page Tree & Path System routing invariant, the Content Versioning source-save boundary, the Frontend Routing URL scheme line, and the breadcrumb-derivation line, per plan.md § Complexity Tracking
- [X] T078 Bump `.specify/memory/constitution.md` to 3.0.0 with a Sync Impact Report — MAJOR, because P8 and three mandate one-line invariants are redefined
- [X] T079 [P] Add the 035 entry to the Recent Changes section of `CLAUDE.md`
- [X] T080 [P] Document the new address fields in `packages/mcp-server/README.md` — `get_page`/`create_page`/`batch_create_pages` tool descriptions updated, plus a new "Page addresses" subsection explaining slug vs. path and noting there is no MCP alias add/remove tool yet
- [X] T081 Wire and test public-content invalidation/warmup in `apps/web/src/server/services/page-addresses.ts`, `pages.ts`, `cross-space-migrations.ts`, and `spaces.ts` plus their tests: a canonical address is served from the cached ISR document; an alias is a cached permanent redirect with no per-request database read; and slug changes, alias add/remove/release, deletion/restore, cross-space moves, and public-prefix changes each invalidate and warm the required route state — the audit found and fixed two real gaps, both in cross-space moves specifically:
  - `cross-space-migrations.ts`'s batch `moveItem` only warmed the *new* address, never the retained old one in the source space — the first reader to follow an old bookmark after a migration hit a cold cache. Fixed to warm both; covered by a new test asserting two distinct routes are warmed.
  - `pages.ts`'s `moveToSpace` (the single-page admin "move to another space" action, `POST /api/admin/pages/[id]/move` — a separate code path from the batch migration flow above, and apparently missed entirely during this feature's original implementation) had **no address handling at all**: it never checked whether the page's `slug` was already taken in the destination space (only checked `path`, so a same-path-different-slug collision would have hit the DB's unique index directly as an unhandled 500), never retained the page's old address as an alias in the source space (silently breaking FR-010 for this path), and warmed nothing. Fixed all three: added an `assertAddressAvailable` check against the target space before moving, retained the old address the same way the batch flow does, and warmed both routes. Covered by three new tests (collision rejection, retention, warmup).

  The other invalidation-only call sites (`removeAlias`/`releaseAddresses` in `page-addresses.ts`, `remove`/soft-delete in `pages.ts`) were confirmed correct as-is: there is nothing meaningful to pre-warm for a route that should now 404. `spaces.ts`'s public-prefix change deliberately relies on invalidation + lazy on-demand regeneration rather than proactive warmup — it can affect every page in a space at once, and enqueuing a warmup job per page at that scale is a different, out-of-scope feature.
- [X] T082 [P] Remove the now-dead `leafSlugFromPath` helpers from `apps/web/src/server/services/pages.ts` and `apps/web/src/server/services/public-content.ts` — already gone; `grep -rn "leafSlugFromPath" apps/web/src` finds no remaining references
- [X] T083 Run the href sweep from quickstart.md and confirm no remaining call site passes a tree path into a reader-href helper — the sweep's matches were all either legitimate non-reader hrefs (edit/history/draft-review/new-page-prefix routes, which are correctly tree-path-addressed admin/editor tooling, not public reader addresses) or already correctly preferring `slug`/`sourcePath` with a defensive `path` fallback. One real inconsistency found and fixed: `Header.tsx`'s language-switcher "Original" link used `pageContext.path` as its no-`routePrefix` fallback instead of `pageContext.sourcePath` (already proven non-null by the enclosing `hasLanguages` guard, and already used that way by the sibling translation-link one line below) — a page whose slug had diverged from its tree path would get a broken "Original" link. Covered by a new test
- [X] T084 `pnpm lint && pnpm typecheck` clean across the workspace — both pass with zero errors/warnings across all three packages (`@next-wiki/shared`, `@next-wiki/web`, `@next-wiki/mcp-server`)
- [X] T085 Run the full `quickstart.md` validation including the backfill drift check — `pnpm db:generate` confirms `No schema changes, nothing to migrate`; the backfill drift query found exactly one row of drift (`ai/dl/transfer-learning`, slug `ai/dl/transfer-learning-zh`), a legitimate prior slug rename from earlier feature testing on this DB, not backfill corruption. Running the full test suite surfaced one genuine 035-caused failure: `app/sitemap.test.ts` predates `sitemap.ts`'s move (in an earlier commit on this branch, `cc3d7ebb`) to `listSpaces()` + `canonicalSpacePath(space, page.slug)`, so its mocks never supplied a space or a `slug` field and it silently exercised a real, unmocked `listSpaces()` DB call. Fixed by mocking `@/server/services/spaces` and adding `slug` to the fixture pages; the expected URLs also needed the `/wiki/` prefix `canonicalSpacePath` now applies. Every other failure surfaced by the full run (`tags-spaces.test.ts`, `Navigator.test.tsx`'s space-tabs assertion, `content-assets-permissions.test.ts`, `database-store.test.ts`, `ai-conversation-context.test.ts`, `ai-tool-runtime.permissions.test.ts`, `ai-tool-registry.test.ts`, plus the two pre-existing ones already known from earlier in this branch's work — `RevisionPreviewDiff.test.ts`, `permissions.test.ts`, and `pages-move.test.ts`'s two link-page tests) was confirmed via `git log main..HEAD` to be untouched by this feature branch (or, for `Navigator.tsx`, touched in a completely different section than the one that fails) — pre-existing and out of scope, not fixed here
- [ ] T086 Full suite with no stray dev servers running: `pnpm --filter @next-wiki/web test`, then `test:e2e`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: no dependencies
- **Foundational (Phase 2)**: depends on Setup — **blocks every user story**
- **US1 (Phase 3)**: depends on Foundational only
- **US2 (Phase 4)**: depends on Foundational; T034–T035 build on US1's resolver (T019)
- **US3 (Phase 5)**: depends on Foundational; T043 touches write paths US1 and US5 also touch
- **US4 (Phase 6)**: depends on Foundational; T063 builds on US1's static-site work (T023)
- **US5 (Phase 7)**: depends on Foundational; T072 overlaps US2's T037 — whichever lands first satisfies both
- **Polish (Phase 8)**: depends on every story you intend to ship

### Within Each User Story

Schema → shared service → resolver/route → API/MCP surface → UI → tests.
Tests may be written first; T012 and T010 in Foundational are deliberately
paired with the code they cover so the rules are pinned before six write paths
start calling them.

### Parallel Opportunities

- T002 and T003 in Setup
- T009/T010 (reserved rules) run parallel to T011/T012 (validation chokepoint) — different files
- Within US1: T014, T015, T024, T025, T026 and all four tests
- Within US2: T040 and T041
- Within US3: T046, T047 and all four tests
- Within US4: T059, T060, T061, T062, and both tests
- Within US5: T073, T074, T075
- Across stories: once Phase 2 is done, US1 / US3 / US5 can proceed in parallel by different people; US2 and US4 are cheapest to start after US1's resolver and static-site work land

**Serialization warning**: T006, T007, T008 (migration run 1) and T038
(migration run 2) must never run concurrently with each other or with another
schema edit. Two people generating migrations at once is exactly how the
snapshot chain broke before.

---

## Parallel Example: User Story 1

```bash
# DTO plumbing and surface exposure together:
Task: "Add slug to LivePage and page-summary types in packages/shared/src/pages.ts"
Task: "Add slug to tree/search/backlink/citation payloads in apps/web/src/server/services/public-content.ts"
Task: "Expose slug and url on v1 read payloads in apps/web/app/api/v1/pages/"
Task: "Expose slug and url on MCP read tools in packages/mcp-server"
Task: "Show the default slug in apps/web/src/components/pages/NewPageDialog.tsx"

# Then all four US1 tests together:
Task: "Default slug and tree-move invariance in apps/web/src/server/services/pages.test.ts"
Task: "Backfill drift is zero in apps/web/src/server/db/content-storage-schema.test.ts"
Task: "Canonical and translation resolution in apps/web/src/server/services/reader-routing.test.ts"
Task: "Playwright: move survives, address bar unchanged"
```

---

## Implementation Strategy

### MVP First (User Story 1 only)

1. Phase 1 Setup
2. Phase 2 Foundational — **critical**, blocks everything
3. Phase 3 US1
4. **STOP and VALIDATE**: publish, move across the tree, confirm the URL never
   moved; confirm the backfill drift check reports zero
5. Shippable. Legacy redirect mechanisms still work untouched, so nothing
   regresses if you stop here.

### Incremental Delivery

1. Setup + Foundational → namespace exists and is DB-enforced
2. + US1 → URLs decoupled from the tree (**MVP**)
3. + US2 → renames safe, one alias mechanism instead of two
4. + US3 → collisions and reserved words rejected with actionable messages
5. + US4 → multiple addresses per page, manageable in the UI
6. + US5 → Wiki.js and archive migration preserve every inbound link
7. Polish → mandate amendment, docs, cleanup, full validation

### Parallel Team Strategy

Everyone lands Setup + Foundational together (one person owns the migration
tasks — see the serialization warning). Then: developer A takes US1 (largest,
on the critical path), developer B takes US3 once US1's write paths settle,
developer C takes US5. US2 and US4 follow US1.

---

## Notes

- `[P]` means different files and no dependency on an incomplete task
- Never hand-author a migration `.sql` or edit `meta/_journal.json` /
  `meta/NNNN_snapshot.json`; only the backfill in T007 is appended to an
  already-generated file
- Stop any preview or manually started dev server before running e2e — leftover
  servers starve the run and the failures look like regressions
- Commit per task or per logical group; keep the mandate amendment (T077/T078)
  in its own commit, separate from implementation
